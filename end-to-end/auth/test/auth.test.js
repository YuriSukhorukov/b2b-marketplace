jasmine.DEFAULT_TIMEOUT_INTERVAL = 999999;

beforeAll(async () => {});

afterAll(async () => {});

describe('Auth e2e', () => {
	test('Signup', async () => {
		expect(true).toBe(true);
  }),
  test('Signin', async () => {
		expect(true).toBe(true);
  })
});